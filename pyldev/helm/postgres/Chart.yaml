apiVersion: v2
name: postgres
description: PostgreSQL backend for LiteLLM (wraps Bitnami chart)
type: application
version: 0.1.0
appVersion: "16.2.0"
keywords:
  - postgresql
  - database
dependencies:
  - name: postgresql
    version: "14.3.3"
    repository: "https://charts.bitnami.com/bitnami"
