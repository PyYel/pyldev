apiVersion: v1
kind: Service
metadata:
  name: litellm-service
spec:
  type: LoadBalancer  # Litellm is exposed for testing
  ports:
  - port: 4000        # Exposed port
    targetPort: 4000  # internal containerPort specified deployment
  selector:
    app: litellm