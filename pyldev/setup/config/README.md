# config

The ``/config`` folder hosts configuration files, such as ``JSON`` or ``TOML``. These files are supposed to be read only, although its use may be wider, from application installation to regular execution purposes.