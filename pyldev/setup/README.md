# Setup

This is a demo repository, that offers a backbone for any type of application.

You may use the ``init_project.bat`` (resp. ``init_project.sh``) file to generate a similar structure in an other Windows (resp. Unix) project folder.